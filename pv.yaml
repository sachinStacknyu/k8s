apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-practice
spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce
  persistantVolumeReClaimPolicy: Retain

  hostPath:
    path: /data/my-storage